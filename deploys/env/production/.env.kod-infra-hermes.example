# GENERATED FROM tenants/kod.yaml — DO NOT EDIT
# Real values live in .env.kod-infra-hermes (gitignored, operator-managed).
# This file documents the env contract; never commit secrets here.

# === Upstream image + identity ===
HERMES_EDITION=superuser
HERMES_UPSTREAM_REF=v2026.5.29
HERMES_LOG_LEVEL=info
HERMES_UID=10000
HERMES_GID=10000
TZ=Asia/Jakarta

# === Coding-agent workspace (superuser only) ===
# Fine-grained GitHub PAT (Contents+PRs RW). Set yourself; never paste in chat.
# OAuth (Claude/ChatGPT) bootstrapped once via scripts/bootstrap-coding-agents.sh.
HERMES_WORKSPACE_GH_PAT=CHANGE_ME

# === Telegram inbound ===
TELEGRAM_BOT_TOKEN=CHANGE_ME
TELEGRAM_ALLOWED_USERS=

# === MCP outbound (always; per-service opt-in via real .env) ===
MCP_ODOO_URL=
MCP_ODOO_AUTH_TOKEN=
MATTERMOST_BASE_URL=
MATTERMOST_BOT_TOKEN=
MATTERMOST_TEAM_ID=
MCP_MATTERMOST_URL=
MCP_MATTERMOST_AUTH_TOKEN=
MAILCOW_BASE_URL=
MAILCOW_API_KEY=
MAILCOW_API_KEY_TYPE=
MCP_MAILCOW_URL=
MCP_MAILCOW_AUTH_TOKEN=
WAHA_BASE_URL=
WAHA_API_KEY=
WAHA_HMAC_KEY=
MCP_WAHA_URL=
MCP_WAHA_AUTH_TOKEN=
