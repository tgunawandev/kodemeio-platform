kind: instance
extends: ../../bases/infra.yaml

instance:
  name: tpp-infra-postgres
  description: "TPP dedicated PostgreSQL 16 database (local mirror of prod)"

project: database
environment: local
server: vbox-ubuntu-server

source_overrides:
  type: github
  owner: tgunawandev
  repo: kodemeio-postgres
  branch: main
  compose_path: ./docker-compose.prod.yml

env_file: ../../env/local/.env.tpp-infra-postgres
