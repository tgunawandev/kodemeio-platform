# =============================================================================
# tpp-infra-stirling — TPP PRODUCTION (sanitized template)
# =============================================================================
# Stirling PDF — self-hosted PDF toolkit (60+ tools, OCR, LibreOffice, REST API).
# SSO via Authentik forward-auth (Traefik edge); Stirling internal login is
# disabled. Native Stirling OAuth2 isn't used because auto-create-user requires
# a paid Pro/Enterprise license.
#
# Copy to .env.tpp-infra-stirling, fill real secrets, then `generate.py` runs
# the deploy pipeline.

# =============================================================================
# Compose
# =============================================================================
COMPOSE_PROJECT_NAME=tpp-infra-stirling
TENANT=tpp

# =============================================================================
# Domain
# =============================================================================
DOMAIN=stirling.idtpp.com

# =============================================================================
# Image
# =============================================================================
STIRLING_IMAGE=stirlingtools/stirling-pdf
STIRLING_TAG=latest-fat

# =============================================================================
# Security
# =============================================================================
SECURITY_CSRFDISABLED=false

# Global API Key — generate with: openssl rand -hex 32 (prefix sk-stirling-).
SECURITY_CUSTOMGLOBALAPIKEY=sk-stirling-change-me-64-hex-chars

# =============================================================================
# Java / JVM Settings
# =============================================================================
JAVA_MIN_HEAP=512m
JAVA_MAX_HEAP=4g

# =============================================================================
# System Configuration
# =============================================================================
SYSTEM_MAXFILESIZE=200
SYSTEM_DEFAULTLOCALE=en_US
SYSTEM_CORSALLOWEDORIGINS=https://stirling.idtpp.com

# =============================================================================
# OCR & Advanced Conversions
# =============================================================================
INSTALL_BOOK_AND_ADVANCED_HTML_OPS=true

# =============================================================================
# Authentik Forward Auth
# =============================================================================
# Internal dokploy-network alias when colocated; fall back to public URL
# (https://auth.idtpp.com) if cross-host.
AUTHENTIK_HOST=http://authentik-server:9000

# =============================================================================
# Resource Limits
# =============================================================================
CPU_LIMIT=2
MEMORY_LIMIT=4G

# =============================================================================
# Logging
# =============================================================================
LOG_MAX_SIZE=50m
LOG_MAX_FILE=3

# =============================================================================
# Timezone
# =============================================================================
TZ=Asia/Jakarta
