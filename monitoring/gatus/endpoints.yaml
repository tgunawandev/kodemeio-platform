# Gatus Endpoint Configuration
# Kodemeio Platform — All monitored services
#
# This file defines all health check endpoints for the Gatus uptime monitor.
# Gatus reads its own config.yaml on the server; this file serves as the
# source-of-truth for what SHOULD be configured.
#
# Gatus config path (on server): /config/config.yaml
# Docs: https://gatus.io/docs

# ---------------------------------------------------------------------------
# Odoo Instances
# ---------------------------------------------------------------------------
endpoints:
  # -- Odoo: kodeme.io --
  - name: "Odoo Production"
    group: "odoo"
    url: "https://odoo.kodeme.io/web/health"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[BODY] == pass"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "Odoo HRMS"
    group: "odoo"
    url: "https://odoo-hrms.kodeme.io/web/health"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[BODY] == pass"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  # -- Odoo: mandiriagro.com --
  - name: "Odoo Trading (MAC)"
    group: "odoo"
    url: "https://odoo.mandiriagro.com/web/health"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[BODY] == pass"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "Odoo HRMS (MAC)"
    group: "odoo"
    url: "https://odoo-hrms.mandiriagro.com/web/health"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[BODY] == pass"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  # -- Odoo: pakerti.com --
  - name: "Odoo Trading (Pakerti)"
    group: "odoo"
    url: "https://odoo.pakerti.com/web/health"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[BODY] == pass"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "Odoo HRMS (Pakerti)"
    group: "odoo"
    url: "https://odoo-hrms.pakerti.com/web/health"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[BODY] == pass"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  # ---------------------------------------------------------------------------
  # Next.js Websites
  # ---------------------------------------------------------------------------
  - name: "kodeme.io"
    group: "websites"
    url: "https://kodeme.io"
    interval: 120s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "mandiriagro.com"
    group: "websites"
    url: "https://mandiriagro.com"
    interval: 120s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "pakerti.com"
    group: "websites"
    url: "https://pakerti.com"
    interval: 120s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "terakidz.com"
    group: "websites"
    url: "https://terakidz.com"
    interval: 120s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "trigunawan.com"
    group: "websites"
    url: "https://trigunawan.com"
    interval: 120s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  # ---------------------------------------------------------------------------
  # Infrastructure Services
  # ---------------------------------------------------------------------------
  - name: "Authentik SSO"
    group: "infrastructure"
    url: "https://auth.kodeme.io/-/health/ready/"
    interval: 30s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 3000"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "Grafana"
    group: "infrastructure"
    url: "https://grafana.kodeme.io/api/health"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 3000"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "GlitchTip"
    group: "infrastructure"
    url: "https://glitchtip.kodeme.io/_health/"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 3000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "Mailcow"
    group: "infrastructure"
    url: "https://mail.kodeme.io"
    interval: 120s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "WAHA (WhatsApp)"
    group: "infrastructure"
    url: "https://waha.kodeme.io/api/health"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 3000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "Tactical RMM"
    group: "infrastructure"
    url: "https://rmm.kodeme.io"
    interval: 120s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "RustDesk"
    group: "infrastructure"
    url: "https://rustdesk.kodeme.io"
    interval: 120s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  # ---------------------------------------------------------------------------
  # Database — TCP check
  # ---------------------------------------------------------------------------
  - name: "PostgreSQL 16"
    group: "database"
    url: "tcp://10.0.0.3:5432"
    interval: 30s
    conditions:
      - "[CONNECTED] == true"
      - "[RESPONSE_TIME] < 500"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2
      - type: slack
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  # ---------------------------------------------------------------------------
  # DNS Resolution Checks
  # ---------------------------------------------------------------------------
  - name: "kodeme.io DNS"
    group: "dns"
    url: "kodeme.io"
    dns:
      query-name: "kodeme.io"
      query-type: "A"
    interval: 300s
    conditions:
      - "[DNS_RCODE] == NOERROR"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "mandiriagro.com DNS"
    group: "dns"
    url: "mandiriagro.com"
    dns:
      query-name: "mandiriagro.com"
      query-type: "A"
    interval: 300s
    conditions:
      - "[DNS_RCODE] == NOERROR"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "pakerti.com DNS"
    group: "dns"
    url: "pakerti.com"
    dns:
      query-name: "pakerti.com"
      query-type: "A"
    interval: 300s
    conditions:
      - "[DNS_RCODE] == NOERROR"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "terakidz.com DNS"
    group: "dns"
    url: "terakidz.com"
    dns:
      query-name: "terakidz.com"
      query-type: "A"
    interval: 300s
    conditions:
      - "[DNS_RCODE] == NOERROR"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "trigunawan.com DNS"
    group: "dns"
    url: "trigunawan.com"
    dns:
      query-name: "trigunawan.com"
      query-type: "A"
    interval: 300s
    conditions:
      - "[DNS_RCODE] == NOERROR"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2

  - name: "kidneuro.io DNS"
    group: "dns"
    url: "kidneuro.io"
    dns:
      query-name: "kidneuro.io"
      query-type: "A"
    interval: 300s
    conditions:
      - "[DNS_RCODE] == NOERROR"
    alerts:
      - type: telegram
        enabled: true
        failure-threshold: 3
        success-threshold: 2
