# GENERATED FROM tenants/mac.yaml — DO NOT EDIT
kind: instance
extends: ../../bases/hermes.yaml
instance:
  name: mac-infra-hermes
  description: Hermes Agent (MAC) — Mattermost inbound (mac-mm.idtpp.com), MCP outbound
project: mac
server: tpp-prod-05
env_file: ../../env/production/.env.mac-infra-hermes
env_overrides:
  HERMES_EDITION: business
  HERMES_UPSTREAM_REF: v2026.5.16
  HERMES_CONTAINER_PREFIX: mac-infra-hermes
  HERMES_CPU_LIMIT: '2.0'
  HERMES_MEM_LIMIT: 2G
  HERMES_MEM_RESERVATION: 512M

# bases/hermes.yaml's backup block targets 'kodemeio-s3-backups' which
# exists on dokploy.kodeme.io but NOT on dokploy.idtpp.com. Backup phase
# fails cosmetically during `deploy apply`; the actual deploy still succeeds.
