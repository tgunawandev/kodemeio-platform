[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kctl-odoo"
version = "0.2.0"
description = "Kodemeio Odoo CLI - manage Odoo 18 ERP instances"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
authors = [{ name = "Kodemeio", email = "dev@kodeme.io" }]
keywords = ["odoo", "erp", "cli", "kodemeio", "json-rpc"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: System :: Systems Administration",
]
dependencies = [
    "kctl-lib>=0.4.0",
    "httpx>=0.28.0",
    "openpyxl>=3.1.5",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-httpx>=0.35.0",
    "pytest-cov>=6.0.0",
    "ruff>=0.9.0",
    "mypy>=1.14.0",
    "types-PyYAML>=6.0.0",
]

[project.scripts]
kctl-odoo = "kctl_odoo.cli:_run"

[tool.uv.sources]
kctl-lib = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/kctl_odoo"]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM"]
# TODO(kctl-odoo debt): the ignores below track pre-existing issues, not a policy choice.
#   B904 (raise ... from): 36 sites — add `from e` / `from None` when refactoring each module.
#   E741 (ambiguous names l/I/O): 22 sites in tax/accounting code where `l` = "line".
#   E501 (line-too-long): 10 long f-strings/docstrings — break when touched.
#   B007 (unused loop var): 5 sites — rename to `_`.
#   SIM102/SIM105/SIM108: prefer as-written for readability in this module.
ignore = ["B904", "E741", "E501", "B007", "SIM102", "SIM105", "SIM108"]

[tool.mypy]
python_version = "3.12"
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: tests that read real YAML files from install/ directory",
    "smoke: tests that require a live Odoo instance",
]


[tool.coverage.run]
source = ["src/kctl_odoo/core", "src/kctl_odoo/commands/generate.py"]
omit = ["*/commands/aliases.py"]

[tool.coverage.report]
show_missing = true
skip_empty = true
