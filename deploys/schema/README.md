# Manifest schema

The executable manifest model is maintained by `kctl-dokploy` in
`kodemeio-cli`. This directory is reserved for exported, versioned schemas used
by editors and offline CI.

An exported schema must record the `kctl-dokploy` version that generated it.
Do not maintain an independent hand-written schema here.
