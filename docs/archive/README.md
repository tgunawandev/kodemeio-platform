# Historical material

This directory preserves implementation plans and generated CLI documentation
from before the 2026-08-25 repository consolidation.

Content here is historical and may reference old repository layouts or CLI
versions. Current operating instructions live in `README.md`, `docs/`, and
`ops/runbooks/`.
