kind: instance
extends: ../../bases/infra.yaml

instance:
  name: kod-infra-postgres
  description: "Shared PostgreSQL 16 database"

project: kod

source_overrides:
  repo: kodemeio-postgres

env_file: ../../env/production/.env.kod-infra-postgres
