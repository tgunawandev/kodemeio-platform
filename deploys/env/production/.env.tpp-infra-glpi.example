# =============================================================================
# GLPI IT Service Management — PRODUCTION TEMPLATE
# =============================================================================
# Copy to .env.prod and fill in all CHANGE_ME_* values.
# Official image: glpi/glpi (https://hub.docker.com/r/glpi/glpi)
# Database: Bundled MariaDB 11 (GLPI does not support PostgreSQL)

# =============================================================================
# Compose Project
# =============================================================================
COMPOSE_PROJECT_NAME=tpp-infra-glpi
TENANT=tpp

# =============================================================================
# Domain Configuration
# =============================================================================
GLPI_DOMAIN=glpi.example.com

# =============================================================================
# GLPI Version
# =============================================================================
GLPI_TAG=10.0.25

# =============================================================================
# GLPI — Database Connection (Bundled MariaDB)
# =============================================================================
GLPI_DB_HOST=glpi-mariadb
GLPI_DB_PORT=3306
GLPI_DB_NAME=glpi
GLPI_DB_USER=glpi
# Generate: openssl rand -base64 24
GLPI_DB_PASSWORD=CHANGE_ME_DB_PASSWORD

# =============================================================================
# GLPI — Auto-Install & Update
# =============================================================================
GLPI_SKIP_AUTOINSTALL=false
GLPI_SKIP_AUTOUPDATE=false
GLPI_CRONTAB_ENABLED=1

# =============================================================================
# MariaDB 11 (Bundled)
# =============================================================================
# Generate: openssl rand -base64 24
MARIADB_ROOT_PASSWORD=CHANGE_ME_ROOT_PASSWORD
MARIADB_DATABASE=glpi
MARIADB_USER=glpi
# Must match GLPI_DB_PASSWORD
MARIADB_PASSWORD=CHANGE_ME_DB_PASSWORD

# =============================================================================
# SMTP
# =============================================================================
GLPI_SMTP_HOST=mail.example.com
GLPI_SMTP_PORT=587
GLPI_SMTP_USERNAME=glpi@example.com
GLPI_SMTP_PASSWORD=CHANGE_ME_SMTP_PASSWORD
GLPI_SMTP_FROM=glpi@example.com
GLPI_SMTP_SENDER_NAME=GLPI Helpdesk

# =============================================================================
# Resource Limits
# =============================================================================
GLPI_CPU_LIMIT=1
GLPI_MEMORY_LIMIT=1G
MARIADB_CPU_LIMIT=0.5
MARIADB_MEMORY_LIMIT=512M

# =============================================================================
# Timezone
# =============================================================================
TZ=Asia/Jakarta
