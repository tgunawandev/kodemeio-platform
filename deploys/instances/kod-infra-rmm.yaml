kind: instance
extends: ../bases/infra.yaml

instance:
  name: kodemeio-rmm
  description: "Tactical RMM remote monitoring"

source_overrides:
  repo: kodemeio-rmm

domain:
  host: rmm.kodeme.io
  port: 8080
  service: tactical-rmm
  https: true
  cert: letsencrypt
