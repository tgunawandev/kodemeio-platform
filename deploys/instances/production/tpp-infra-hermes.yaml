# GENERATED FROM tenants/tpp.yaml — DO NOT EDIT
kind: instance
extends: ../../bases/hermes.yaml
instance:
  name: tpp-infra-hermes
  description: Hermes Agent (Pakerti) — Mattermost inbound (mm.idtpp.com), MCP outbound
project: tpp
server: tpp-prod-04
env_file: ../../env/production/.env.tpp-infra-hermes
env_overrides:
  HERMES_EDITION: superuser
  HERMES_UPSTREAM_REF: v2026.8.31
  HERMES_CONTAINER_PREFIX: tpp-infra-hermes
  HERMES_CPU_LIMIT: '4.0'
  HERMES_MEM_LIMIT: 6G
  HERMES_MEM_RESERVATION: 1G

# bases/hermes.yaml's backup block targets 'kodemeio-s3-backups' which
# exists on dokploy.kodeme.io but NOT on dokploy.idtpp.com. Backup phase
# fails cosmetically during `deploy apply`; the actual deploy still succeeds.
