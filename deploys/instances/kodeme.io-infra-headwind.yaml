kind: instance
extends: ../bases/infra.yaml

instance:
  name: kodemeio-headwind
  description: "Headwind MDM device management"

source_overrides:
  repo: kodemeio-headwind
  branch: "master"

domain:
  host: mdm.kodeme.io
  port: 8080
  service: headwind
  https: true
  cert: letsencrypt
