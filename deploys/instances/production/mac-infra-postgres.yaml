kind: instance
extends: ../../bases/infra.yaml

instance:
  name: mac-infra-postgres
  description: "MAC dedicated PostgreSQL 16 database (tpp-prod-02)"

project: mac
environment: production
server: tpp-prod-02

source_overrides:
  type: github
  owner: tgunawandev
  repo: kodemeio-postgres
  branch: main
  compose_path: ./docker-compose.prod.yml

env_file: ../../env/production/.env.mac-infra-postgres
