# GENERATED FROM tenants/tpp.yaml — DO NOT EDIT
kind: instance
extends: ../../bases/infra.yaml
instance:
  name: tpp-hono-notify
  description: Notification dispatch service (FCM push + SSE real-time + Telegram)
source_overrides:
  type: github
  owner: tgunawandev
  repo: kodemeio-react
  branch: main
  compose_path: apps/api/notify/docker-compose.yml
project: tpp
environment: staging
server: kod-stg-01
healthcheck:
  path: /health
  port: 3020
  expected_status: 200
  timeout: 60
  interval: 10
dns:
  zone: pakerti.com
  name: stg-tpp-notify
domain:
  host: stg-tpp-notify.pakerti.com
  port: 3020
  service: notify
  https: true
env_overrides:
  NODE_ENV: production
  PORT: '3020'
  DATABASE_URL: postgresql://kodemeio:${DB_PASSWORD}@postgres:5432/kodemeio
  REDIS_URL: redis://redis:6379
  DISPATCH_SECRET: ${DISPATCH_SECRET}
  JWT_SECRET: ${JWT_SECRET}
  ALLOWED_ORIGINS: https://stg-tpp-sfa.pakerti.com,https://stg-tpp-bia.pakerti.com,https://stg-tpp-wms.pakerti.com,https://stg-tpp-hrm.pakerti.com
  FIREBASE_SERVICE_ACCOUNT_JSON: ${FIREBASE_SERVICE_ACCOUNT_JSON}
