kind: instance
extends: ../bases/infra.yaml

instance:
  name: kod-infra-glitchtip
  description: "GlitchTip error tracking"

source_overrides:
  repo: kodemeio-glitchtip

domain:
  host: glitchtip.kodeme.io
  port: 8080
  service: web
  https: true
  cert: letsencrypt
