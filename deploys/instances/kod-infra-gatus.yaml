kind: instance
extends: ../bases/infra.yaml

instance:
  name: kod-infra-gatus
  description: "Gatus health monitoring"

project: kod

source_overrides:
  repo: kodemeio-gatus

domain:
  host: gatus.kodeme.io
  port: 8080
  service: gatus
  https: true
  cert: letsencrypt

env_file: ../env/.env.kod-infra-gatus
